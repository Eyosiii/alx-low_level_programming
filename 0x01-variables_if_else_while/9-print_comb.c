#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints numbers with symbol
 *
 * Return: 0
 */

int main(void)
{
	int x;

	for (x = 48; x <= 57; x++)
	putchar(x);
	putchar(44);
	putchar(32);
	return (0);
}
