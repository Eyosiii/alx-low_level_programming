#include <stdio.h>

/**
 * main - entry point
 *
 * Description: Prints all single digits
 *
 * Return: 0
 */

int main(void)
{
	char i;

	for (i = 0 ; i < 10 ; i++)
	printf("%d", i);
	printf("\n");
	return (0);
}
