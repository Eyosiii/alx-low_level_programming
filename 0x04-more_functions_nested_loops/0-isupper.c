#include "main.h"

/**
 * _isupper - shows if the letters are upper or not
 *@c: character used to check
 *
 * Return: 0
 */

int _isupper(int c)
{
	char x;

	if (x >= 65 && x <= 90)
		_putchar('1');
	else
		_putchar ('0');
	return (0);
}
