#include "main.h"

/* main - Entry point
 *
 * Description: Shows the absolute value of integers
 *
 * Return: 0
 */

int _abs(int i)
{	
	if ((i = (-i)))
	_putchar (i);
	else
	_putchar (i);	
	return (0);
}
